@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>İlan Verme Sayfası</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Arka plan resmini bulanıklaştırma */
        .blurred-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('sakarya-emlak-sitesi_20200301232605924cqjyu.jpg');
            background-size: cover;
            background-position: center;
            filter: blur(5px);
            z-index: -1;
        }

        /* Başlık İçin Animasyon ve Stil */
        .title-container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            z-index: 999;
            animation: moveTitle 2s ease-out forwards;
        }

        .title {
            font-size: 4rem;
            font-weight: bold;
            color: #ff0000;
        }

        .subtitle {
            font-size: 1.5rem;
            font-weight: bold;
            color: #ffffff;
            margin-top: 10px;
        }

        @@keyframes moveTitle {
            0% {
                top: 20%;
                transform: translate(-50%, -50%);
            }

            100% {
                top: 10%;
                transform: translate(-50%, -10%);
            }
        }

        /* İlan Verme Formu Stili */
        .form-container {
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            margin-top: 15rem;
        }

            .form-container label {
                font-weight: bold;
            }

            .form-container button {
                background-color: #ff0000;
                color: white;
                font-weight: bold;
            }

                .form-container button:hover {
                    background-color: #cc0000;
                }

        /* Ana Sayfa Butonu */
        .home-button {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 1000;
        }

            .home-button button {
                font-size: 1.25rem;
                font-weight: bold;
                padding: 10px 20px;
                background-color: #007bff;
                color: white;
                border: none;
                border-radius: 5px;
                box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            }

                .home-button button:hover {
                    background-color: #0056b3;
                }
    </style>
</head>
<body>
    <div class="blurred-background"></div>

    <div class="title-container">
        <div class="title">PropPulse Emlak</div>
        <div class="subtitle">Hadi, ilanını yayınla!</div>
    </div>

    <div class="container">
        <div class="form-container mx-auto col-md-8">
            <h3 class="text-center">İlan Ver</h3>
            <form asp-action="Olustur" asp-controller="Ilanlar" method="post">
                <div class="mb-3">
                    <label for="ilanAdi" class="form-label">İlan Adı</label>
                    <input type="text" class="form-control" id="ilanAdi" name="ilanAdi" placeholder="İlan adını giriniz">
                </div>
                <div class="mb-3">
                    <label for="adres" class="form-label">Evin Adresi</label>
                    <input type="text" class="form-control" id="adres" name="adres" placeholder="Evin adresini giriniz">
                </div>
                <div class="mb-3">
                    <label for="odaSayisi" class="form-label">Kaç Artı Kaç</label>
                    <input type="text" class="form-control" id="odaSayisi" name="odaSayisi" placeholder="Örneğin: 3+1">
                </div>
                <div class="mb-3">
                    <label for="ilanTuru" class="form-label">Kiralık mı Satılık mı?</label>
                    <select class="form-select" id="ilanTuru" name="ilanTuru">
                        <option value="">Seçiniz</option>
                        <option value="Kiralık">Kiralık</option>
                        <option value="Satılık">Satılık</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="esyaDurumu" class="form-label">Eşya Durumu</label>
                    <select class="form-select" id="esyaDurumu" name="esyaDurumu">
                        <option value="">Seçiniz</option>
                        <option value="Eşyalı">Eşyalı</option>
                        <option value="Eşyasız">Eşyasız</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="fotograflar" class="form-label">Fotoğraflar</label>
                    <input type="file" class="form-control" id="fotograflar" name="fotograflar" multiple>
                </div>
                <div class="mb-3">
                    <label for="brutMetrekare" class="form-label">Brüt Metrekare</label>
                    <input type="number" class="form-control" id="brutMetrekare" name="brutMetrekare" placeholder="Metrekareyi giriniz">
                </div>
                <div class="text-center">
                    <button type="submit" class="btn btn-primary">İlanı Yayınla</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>